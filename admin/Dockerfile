FROM node:20-alpine

WORKDIR /app

# Non-interactive install if package.json exists
# For now, just a placeholder to keep docker-compose happy
RUN echo "No package.json yet" > info.txt

EXPOSE 5174

CMD ["sh", "-c", "while true; do sleep 1000; done"]
